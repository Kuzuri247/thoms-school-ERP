{
  "name": "thomson-school-erp",
  "version": "1.0.0",
  "scripts": {
    "start": "npm start --prefix backend & npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "dependencies": {
    "axios": "^1.18.1"
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
